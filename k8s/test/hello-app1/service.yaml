apiVersion: v1
kind: Service
metadata:
  name: service
  annotations:
    # must be created security policy in advance.
    cloud.google.com/backend-config: '{"ports": {"server":"test-app1-backendconfig"}}'
